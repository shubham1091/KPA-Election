{
  "buildCommand": "cd ../.. && npx turbo run build --filter=stv-election-voter",
  "outputDirectory": "dist",
  "installCommand": "npm install --prefix ../..",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

