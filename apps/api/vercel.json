{
  "version": 2,
  "builds": [
    {
      "src": "api/index.cjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.cjs"
    }
  ],
  "buildCommand": "cd ../.. && npm install --include=dev && cd apps/api && npm run build",
  "installCommand": "npm install --prefix=../.."
}

